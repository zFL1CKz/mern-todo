import React, {FC} from 'react'

interface NavItemProps {
  text: string
  active?: boolean
}

const NavItem: FC<NavItemProps> = ({text, active  }) => {
  return (
    <div>

    </div>
  )
}

export default NavItem